# FCM SERVICE

Firebase Cloud Messaging is used to send notification to customer device.

# RULES

fcm service is one of notification provider that can be find in [Notification Provider](../../internal/service/notification/provider.go). to avoid error when using FCM you MUST to adjust FCM implementation according with Notification Provider
