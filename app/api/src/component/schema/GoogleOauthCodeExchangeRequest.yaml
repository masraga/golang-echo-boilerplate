type: object
required: 
  - actions
properties:
  actions:
    type: array
    description: array of string action that could be happen when code is authorized
    example: ["REGISTER_USER"]
    items:
      $ref: ./GoogleOauthCodeExchangeActionEnum.yaml